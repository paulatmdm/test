import { OnInit, OnDestroy, ElementRef, EventEmitter, Renderer2, NgZone } from '@angular/core';
/**
 * @hidden
 */
export declare class EventsOutsideAngularDirective implements OnInit, OnDestroy {
    private element;
    private ngZone;
    private renderer;
    eventNames: string | string[];
    domEventHandler: EventEmitter<any>;
    protected subscriptions: any[];
    constructor(element: ElementRef, ngZone: NgZone, renderer: Renderer2);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private emitEvent(args);
}
